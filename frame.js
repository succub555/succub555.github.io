        // Open the iframe in a container
        function openFrame() {
            document.getElementById('frame').style.display = 'block';  // Show the iframe container
        }

        // Close the iframe container
        function closeFrame() {
            document.getElementById('frame').style.display = 'none';  // Hide the iframe container
        }